cmake_minimum_required(VERSION 3.23)
project(distance_transform_benchmark C CXX)

option(BUILD_TESTING "Build the tests of the package" OFF)
option(BUILD_PYTHON "Build the python extension (should only be handled by scikit-build-core)" OFF)

add_subdirectory(dt)
add_subdirectory(bin)
if (BUILD_TESTING)
    enable_testing()
    add_subdirectory(tests)
endif(BUILD_TESTING)
if (BUILD_PYTHON)
    add_subdirectory(python/cxx)
endif(BUILD_PYTHON)